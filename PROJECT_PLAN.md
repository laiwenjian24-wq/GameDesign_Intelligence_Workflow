# PROJECT_PLAN

## 项目定位

GameDesign Intelligence Workflow 是一个面向游戏文案策划和叙事设计的 AI 辅助工作流。

第一阶段只服务《STUPID: Before Judgment Day》的 Narrative Workflow，不做完整制作管线，不做通用知识管理平台，不做网页产品。

项目目标是把分散的世界观、角色、剧情、草稿、灵感和叙事模式资料转化为可检索、可追踪、可检查的叙事资产库。

## 第一阶段 MVP 范围

第一版必须完成的最小闭环：

```text
资料导入 -> 自动分类 / metadata -> 检索 -> 连续性检查 -> 输出报告
```

### 1. 资料导入

支持格式：

- Markdown
- TXT
- PDF
- PNG
- JPG / JPEG

第一版假设用户把资料放入本地输入目录，系统扫描并导入。

暂不要求用户提前整理成统一模板。系统需要尽量从原始文件中提取标题、正文、摘要、文件来源和基础属性。

### 2. 自动分类

资料分类固定为：

- Canon
- Draft
- Inspiration
- Deprecated
- Pattern
- Unknown

Unknown 是必要状态。模型不确定时必须允许输出 Unknown，而不是强行归类。

### 3. metadata 生成

每个资料至少生成：

- 文档 ID
- 文件路径
- 文件名
- 文件类型
- 资料分类
- 标题
- 摘要
- 相关角色
- 相关地点
- 时间线引用
- 主题标签
- 项目相关性
- Canon 置信度
- 导入时间
- 内容哈希

metadata 的目的不是装饰，而是为后续检索、过滤、冲突检查和报告引用服务。

### 4. 检索

第一版检索需要支持：

- 按自然语言问题检索。
- 按资料分类过滤。
- 返回来源文件。
- 返回相关片段。
- 标明结果来自 Canon、Draft、Inspiration、Deprecated 还是 Pattern。

检索结果不能把 Inspiration 当成 Canon。

### 5. 剧情连续性检查

输入可以是一段剧情草稿、场景大纲或设定描述。

输出应包括：

- 可能冲突的 Canon。
- 缺少依据的设定。
- 时间线疑点。
- 角色动机疑点。
- 需要人工确认的问题。
- 引用到的来源资料。

第一版不追求自动判断一切，只要求能把风险点暴露出来。

### 6. 输出报告

报告需要可读、可保存、可复核。

建议输出 Markdown 报告，内容包括：

- 输入资料摘要。
- 检索到的关键依据。
- 冲突或疑点列表。
- 风险等级。
- 建议处理方式。
- 来源引用。

## 明确不做

第一阶段不做以下内容：

- MCP。
- 多 Agent。
- 网页 UI。
- 自动剧情生成器。
- 自动改写正式设定。
- Ren'Py 脚本解析。
- Excel 解析。
- 完整知识图谱。
- 复杂权限系统。
- 多用户协作。
- 云端部署。
- 生产级监控。

这些功能会扩大工程面，干扰第一阶段目标。

## 推荐里程碑

### Milestone 0：文档启动

产物：

- README
- 项目计划
- 架构说明
- 工作流规格

目标：

- 固定第一阶段范围。
- 明确不做事项。
- 建立术语和资料分类标准。

### Milestone 1：资料规范与目录骨架

产物：

- 本地目录结构。
- metadata schema。
- 分类规则。
- 示例资料。

目标：

- 让系统知道什么是 Canon、Draft、Inspiration、Deprecated、Pattern。

### Milestone 2：资料导入与 metadata

产物：

- 本地文件扫描。
- 文本提取。
- 图片基础处理。
- metadata 生成。
- 内容哈希去重。

目标：

- 资料进入系统后可以被识别、记录和复查。

### Milestone 3：检索闭环

产物：

- 本地向量索引。
- 语义检索。
- metadata 过滤。
- 来源引用。

目标：

- 用户可以查询项目资料，并看到回答依据。

### Milestone 4：连续性检查与报告

产物：

- 剧情连续性检查命令。
- Markdown 报告输出。
- 风险分级。

目标：

- 对新剧情草稿进行基于 Canon 的风险检查。

## 成功标准

第一版成功标准：

- 用户可以把混杂资料放入输入目录。
- 系统可以导入资料并生成 metadata。
- 系统可以给出分类建议。
- 用户可以检索资料并看到来源。
- 用户可以提交一段剧情草稿并得到连续性检查报告。
- 报告能区分 Canon 依据、Draft 参考和 Inspiration 灵感。

## 主要风险

### Canon 污染

风险：灵感资料或草稿被模型误认为正式设定。

处理：检索和检查时必须显示资料分类；Canon 变更必须人工确认。

### 分类不稳定

风险：同一类资料多次导入得到不同分类。

处理：记录分类原因、置信度和人工复核结果。

### 图片资料价值不足

风险：PNG/JPG 只有文件 metadata，无法真正参与叙事检索。

处理：第一版只做基础 caption 或 OCR；复杂图像理解后置。

### 过早工程化

风险：过早做 UI、多 Agent、MCP 或云部署，导致核心工作流迟迟不能闭环。

处理：第一阶段只做本地 Narrative Workflow。

